# Bundle-structure gate calibration (local, $0)

Run 2026-09-07 against copies of the repo's real bundle.md / behaviors/gitea.yaml /
skills/gitea/SKILL.md in /tmp, mutating one thing per case. Script extracted verbatim
from the heredoc in .github/workflows/ci.yml.

An instrument that cannot come out both ways has not been tested.

```
rc=0  expected=0  AS EXPECTED  -- control (unmodified)
        Bundle structure OK.
rc=1  expected=non-zero  AS EXPECTED  -- empty skill description
        - skills/gitea/SKILL.md: missing or empty description (skill triggers live here)
rc=1  expected=non-zero  AS EXPECTED  -- broken behaviors YAML
        ^
rc=1  expected=non-zero  AS EXPECTED  -- bundle.md frontmatter unclosed
        - bundle.md: frontmatter opened but never closed
rc=1  expected=non-zero  AS EXPECTED  -- behaviors/*.yaml removed
        - behaviors/*.yaml matched NOTHING -- a structure gate that checks zero files is decoration
rc=1  expected=non-zero  AS EXPECTED  -- skills/ removed
        - skills/*/SKILL.md matched NOTHING -- a structure gate that checks zero files is decoration
rc=1  expected=non-zero  AS EXPECTED  -- bundle.md missing bundle.name
        - bundle.md frontmatter: missing bundle.name
```
